.col-sm-8.col-md-6.col-lg-6
  %h2
    Sign Up

  = form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :class => "form-horizontal", :role => "form" }) do |f|
    = devise_error_messages!

    .form-group
      = f.label :email, :class => "control-label col-sm-5"
      .col-sm-7
        = f.email_field :email, :class => "form-control", :placeholder => "Email", :autofocus => true

    .form-group
      = f.label :password, :class => "control-label col-sm-5"
      .col-sm-7
        = f.password_field :password, :class => "form-control", :placeholder => "Password"

    .form-group
      = f.label :password_confirmation, :class => "control-label col-sm-5"
      .col-sm-7
        = f.password_field :password_confirmation, :class => "form-control", :placeholder => "Confirm Password"

    .form-group
      .col-sm-offset-5.col-sm-7
        = f.submit "Sign up", :class => "btn btn-block btn-warning c2a-large"

  = render "devise/shared/links"
